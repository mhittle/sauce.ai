[project]
name = "sauce-datasets"
version = "0.1.0"
description = "sauce.ai/datasets — LLM-driven medical dataset finder"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py311"
